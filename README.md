# JavaLabs5-6

The task 5 was to cover over 80% of the code from task 3-4 with tests 

The task 6 was to add few getHeaders method which returns field names of a class and toCSV method which returns field values
Both has to be overrided in subclass
also i had to create a Writer class which has method that calls getHeaders and toCSV and return values to results.csv file. also i needed to create a test for Writer class
Additionally i needed to put checkstyle and spotbugs plugins in pom.xml and correct the code following the reccomendations of these plugins